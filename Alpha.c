#include<stdio.h>
int main()
{
char a;
printf("Enter a character");
scanf("%c",&c);
if((c>=a && c<=z) || (c>=A &&c<=Z))
printf("/nThe character is alphabet");
else
printf("/nThe character is not an alphabet");
return 0;
}
